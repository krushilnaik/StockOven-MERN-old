/**
 * Since we have multiple components trying to work with each other
 * 	(Reddit, ApexCharts, Movers)
 * We'll need to use Redux to store information that both will need
 * instead of just storing everything in each component's state
 */
